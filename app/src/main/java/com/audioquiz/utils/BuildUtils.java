package com.audioquiz.utils;

import com.audioquiz.BuildConfig;

public class BuildUtils {

/*    public static String getEnvironmentVariable(BuildProductFlavor flavor, String variableName) {
        String key = flavor.getFlavourName().toUpperCase() + "_" + variableName;
        return System.getProperty(key);
    }*/
}