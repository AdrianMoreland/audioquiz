package com.audioquiz.navigation;

/*
import com.google.common.reflect.TypeToken;
import com.google.firebase.firestore.DocumentSnapshot;
import com.google.gson.Gson;

import java.util.HashMap;
import java.util.Map;

public class FirestoreMapper {

    public static <T> T deserialize(DocumentSnapshot documentSnapshot, Class<T> clazz) {
        return documentSnapshot.toObject(clazz);
    }

    public static Map<String, Object> serialize(Object object) {
        return new Gson().fromJson(new Gson().toJson(object), new TypeToken<HashMap<String, Object>>() {}.getType());
    }
}
*/
